import { View } from "native-base";
import React from "react";
import MapComponent from "./MapComponent";

const Map = () => (
  <View style={{ flex: 1 }}>
      <MapComponent />
  </View>
);

export default Map;